<%- include('header') %>

<h3>My wax</h3>
<% if(goodstuff) { %>

  <table>
    <tr>
      <th>ID</th>
      <th>Release</th>
      <th>Year</th>
      <th>My notes</th>
      <th>Options</th>
    </tr>
    <% 
    goodstuff.forEach(copy => { 
      let clip = 30;
      let trunc;
      if (copy.ownercomment == null) {
        trunc = '';
      } else if (copy.ownercomment.length<clip) {
        trunc = copy.ownercomment;
      } else {
        trunc = copy.ownercomment.slice(0,clip-1) + '…';
      }
    %>

    <tr id="V<%= copy.owned_release_id %>" style="display: table-row">
      <td><%= copy.owned_release_id %></td>
      <td><%= copy.releasename %></td>
      <td><%= copy.year %></td>
      <td><%= trunc %></td>
      <td><a class="button" onclick="var id = <%= copy.owned_release_id %>; edit(id);">Edit</a></td>
    </tr>
    <form method="POST" action="/editcopy">
    <tr id="E<%= copy.owned_release_id %>" style="display: none">
      <td><%= copy.owned_release_id %></td>
      <td><%= copy.releasename %></td>
      <td><%= copy.year %></td>
      <td>
        <!-- <input name="ownercomment" type="textarea" value="<%= copy.ownercomment %>"> -->
        <textarea name="ownercomment" rows="5" cols="25"><%= copy.ownercomment %></textarea>
      </td>
      <td>
        <input type="hidden" id="owned_release_id" name="owned_release_id" value="<%= copy.owned_release_id %>">
        <input type="submit" name="toupdate" value="Save" />
        <input type="submit" name="todelete" value="Delete" />
        <a class="button" onclick="var id = <%= copy.owned_release_id %>; closeedit(id);">Close</a>
      </td>
    </tr>
    </form>
    <% }) %>
  </table>

<% } %>

<a class="button" href="/collector?id=<%= user_id %>">My page</a>

<%- include('footer') %>

<script src="editbuttonSCRIPT.js"></script>